<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    >

    <AutoCompleteTextView
        android:id="@+id/city_search_activity_auto_complete_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_gravity="center"
        android:hint="Write down a city's name"
        android:maxLines="1"
        android:padding="24dp"
        android:textSize="30dp"
        />

    <ProgressBar
        android:id="@+id/city_search_activity_spinner"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/city_search_activity_auto_complete_text_view"
        android:layout_centerInParent="true"
        />

</RelativeLayout>